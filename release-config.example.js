// Release build configuration template.
// Copy to release-config.js during packaging and replace the placeholder
// with the Parqet Client ID approved for the public integration.
//
// Client IDs are public OAuth identifiers, not secrets.
export const RELEASE_CONFIG = Object.freeze({
  parqetClientId: "__PARQET_CLIENT_ID__"
});
